apiVersion: apps/v1
kind: Deployment
metadata:
  name: javi-nginx
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: javi-nginx
  template:
    metadata:
      labels:
        app: javi-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:1.25.4
          ports:
            - containerPort: 80
